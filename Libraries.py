from math import * #contains all math functions which are sqrt, sin, cos, tan, log, exp, floor, ceil, 
from string import * #contains all string functions which are lower, upper, title, capitalize, etc.
from random import * #contains all random functions which are randint, random, choice, shuffle, etc.
from time import * #contains all time functions which are sleep, time, localtime, strftime, etc.
from datetime import * #contains all datetime functions which are date, time, datetime, timedelta, etc.
from os import * #contains all os functions which are path, mkdir, rmdir, remove, rename, etc.
from sys import * #contains all sys functions which are argv, exit, path, modules, etc.
from re import * #contains all regex functions which are search, match, findall, sub, split, etc.